// Test script to verify patient data generation
console.log('Testing patient data generation...');\n\n// Clear existing data\nlocalStorage.removeItem('medischedule_patients');\nlocalStorage.removeItem('medischedule_appointments');\nlocalStorage.removeItem('medischedule_calls');\n\n// Import and test\nimport { db } from '../services/db';\n\n// Generate fresh data\ndb.generateFreshData();\n\n// Test data\ndb.getPatients().then(patients => {\n  console.log('Generated patients:', patients.length);\n  console.log('Sample patient:', patients[0]);\n});\n\ndb.getAppointments().then(appointments => {\n  console.log('Generated appointments:', appointments.length);\n  console.log('Sample appointment:', appointments[0]);\n});